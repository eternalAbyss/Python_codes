# Immutable ordered sequence of elements

# Declaration 
dimensions = 52, 40, 100

# Unpacking the data
length, width, height = dimensions 

# Printing the data
print('The length : {} Breadth : {} and Height : {} are all well defined'.format(length, width, height))
print(dimensions)